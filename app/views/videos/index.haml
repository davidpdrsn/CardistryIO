= render partial: "filter_and_sort", locals: { filter_submit_path: @filter_submit_path }

- if @videos.present?
  - @videos.each do |video|
    %ul
      %li= link_to video.name, video
- else
  You have no videos, yet
